You are given the heads of two sorted linked lists list1 and list2.

Merge the two lists into one sorted list. The list should be made by splicing together the nodes of the first two lists.
Return the head of the merged linked list.

Example 1:
Input: list1 = [1,2,4], list2 = [1,3,4]
Output: [1,1,2,3,4,4]

Example 2:
Input: list1 = [], list2 = []
Output: []

Example 3:
Input: list1 = [], list2 = [0]
Output: [0]



class Solution 
{
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) 
    {
        // 1. Create a "Dummy" node to act as the starting point.
        // This simplifies the code so we don't have to handle the "head" as a special case.

        ListNode dummy = new ListNode(-1);
        ListNode current = dummy;          // 'current' is our pointer to build the new list

        while (list1 != null && list2 != null)    // 2. Loop while BOTH lists have nodes remaining
        {
            if(list1.val <= list2.val)    // Compare values. Attach the smaller node to our result.
            {
                current.next = list1;    // Point current to list1
                list1 = list1.next;      // Move list1 forward
            }
            else
            {
                current.next = list2;    // Point current to list2
                list2 = list2.next;      // Move list2 forward
            }
            current = current.next;      // Advance our building pointer
        }
        // 3. At this point, one list is empty, but the other might still have nodes.
        // We simply attach the remainder of the non-empty list to the end.

        if(list1 != null)
        {
            current.next = list1;
        }
        else
        {
            current.next = list2;
        }
        return dummy.next;
    }
}
